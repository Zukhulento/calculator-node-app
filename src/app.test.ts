
// The describe function is a way to group tests in jest
describe("Tests in app.ts", () => {
    // The test function is a way to create a test in jest
    test("Should return true", () => {
        // The expect function is a way to assert that the result of the test is what you expect
        expect(true).toBe(true);
    })
});
